# dashboards/

One subdirectory per Grafana folder, mirroring the folder tree in the Grafana UI.
One dashboard per file, named `<uid>.json`.

See [docs/dashboard-conventions.md](../docs/dashboard-conventions.md) before adding or editing anything here.
